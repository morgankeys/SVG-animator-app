/** Left panel: collapsible element tree projected from the markup buffer (Phase 2). */
export function ElementsPanel() {
  return (
    <section className="panel">
      <header className="panel-header">Elements</header>
      <div className="panel-body placeholder">Element tree appears here (Phase 2)</div>
    </section>
  );
}
