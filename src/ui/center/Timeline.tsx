/** Timeline strip: animation/transition rows with play + scrub (Phase 6). Static shell. */
export function Timeline() {
  return (
    <section className="timeline panel">
      <header className="panel-header">Timeline</header>
      <div className="panel-body placeholder">Animation rows appear here (Phase 6)</div>
    </section>
  );
}
